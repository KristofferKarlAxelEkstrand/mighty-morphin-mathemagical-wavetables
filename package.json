{
  "name": "mighty-morphin-mathemagical-wavetables",
  "version": "0.9.0",
  "description": "Mighty Morphin Mathemagical Wavetables - create wavetables by using mathematical functions and morph between them seamlessly.",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "markdownlint": "^0.40.0",
    "markdownlint-cli": "^0.47.0",
    "npm-check-updates": "^19.3.1",
    "prettier": "^3.7.4"
  },
  "scripts": {
    "prepare": "husky",
    "lint:md": "markdownlint \"**/*.md\" --ignore node_modules",
    "lint:md:fix": "markdownlint \"**/*.md\" --fix --ignore node_modules",
    "lint:py": "python -m ruff check wavetable_synthesis/ wavetable_generators/ wavetable_tests/",
    "lint:py:fix": "python -m ruff check --fix wavetable_synthesis/ wavetable_generators/ wavetable_tests/ && python -m ruff format wavetable_synthesis/ wavetable_generators/ wavetable_tests/",
    "type:check": "python -m mypy wavetable_synthesis/",
    "format": "prettier --write \"**/*.{md,json,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{md,json,yml,yaml}\"",
    "update": "npx npm-check-updates && npx npm-check-updates -u && npm install"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "lint-staged": {
    "*.md": [
      "prettier --write",
      "markdownlint --fix"
    ],
    "*.{json,yml,yaml}": [
      "prettier --write"
    ],
    "*.py": [
      "python -m ruff format",
      "python -m ruff check --fix"
    ]
  }
}
